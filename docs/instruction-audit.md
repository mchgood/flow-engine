# 项目指令审计记录

日期：2026-09-06。代码基线：`a97db33206de7f6b899a20dafbaa888738d3f4a8`。

## 来源与范围

依据 [GPT-6 Astra 官方模型指南](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra) 的 Prompting best practices：审计技能及文件指令，明确指令优先级与授权，完成已授权工作，并按改动风险安排验证。本次将这些建议应用于项目，不复制整套人格、代理委派或 API 迁移提示词。

递归检查项目文件（排除构建输出和 Git 元数据），仅发现根目录 `AGENTS.md`；没有项目自有 `SKILL.md` 或 `references/`。因此没有创建空技能或修改环境安装的技能。实际引用链为 README 和 docs 下的需求、设计、快速使用、Boot 接入、测试覆盖说明；检查了其相关指令、链接和契约描述。

## 修改与保留

| 问题 | 处理 | 保留的边界 |
| --- | --- | --- |
| 模块依赖、文档同步、测试流程分散重复 | AGENTS 合并为架构表、业务约束、按任务读取的引用和验证规则 | core 无 Spring；普通 Spring 无 Boot；覆盖模块仅构建使用 |
| 所有提交必须 test，发布再 verify | 文档改动检查链接/契约；代码及构建提交、发布运行 verify 和覆盖率脚本；开发中运行相关测试 | verify 已含 test；CI 不变，LINE 95% / BRANCH 88% 不降低 |
| 未明确操作授权是否跨轮次有效 | 明确沿用当前会话适用授权，已覆盖的动作不反复确认 | 推送不等于强推/发布授权；文件不能自行授权远端写入、破坏操作或对外消息 |
| 需求仍要求只评审、不编码、确认后才进入设计 | 删除失效的阶段阻断，区分需求约束和验证完成状态 | 不将未来规划自动作为实施授权，不将测试存在等同于全部验收完成 |
| 设计仍称尚未实现、实现前需确认 | 更新已有实现状态，保留性能验证与并发覆盖的局限 | 已确定的 singleton、同步协调和终止语义不重复审批 |
| 并行汇合只等“active branches”含糊 | 按运行时澄清：全部输入路径；全不激活跳过，混合状态失败 | 未改运行时；排他汇合继续按边解析及单激活路径处理 |
| “旧图兼容”把现有 DAG 能力写成历史负担 | 改为直接描述支持的多入/多出依赖并行 | 未删除隐式依赖并行能力 |
| NodeContext 摘要误写为接口，异常类并不存在 | 去掉重复类型定义，链接 final 类源码，改为真实 FlowException 错误码及 ClassCastException | null、无输出、祖先可见性和运行时类型检查保持原样 |

业务约束核对：纯框架范围；Mermaid 子集/起止节点；泛型节点输出；单下划线别名；SpEL 唯一匹配/default/只读访问；成对排他区域与跳过；并行汇合；子流程隔离、引用环和事件协调；依赖就绪调度；锁外业务执行；物理退出与迟到结果；Starter 退让和关闭；中文 Javadoc、record 参数、配置单位和浅/深不可变说明均保留。

AGENTS 从 98 行 / 1,069 个空白分隔词缩至 50 行 / 873 个空白分隔词，词数减少约 18.3%。行数受排版影响，词数也不是模型 token 数；未将篇幅变化当作质量或性能证明。

## 验证结果

- 项目指令文件清单与引用链核对完成；修改文件中的本地 Markdown 链接全部存在。
- 对比修改前后差异，改动限于 AGENTS、需求、技术方案及本审计记录；未改 Java、POM、测试、CI、模型/API 配置或覆盖率门槛。
- 对照 DefaultFlowEngine 的 ready/active 判定及 NodeContext 源码，确认本次修正的描述与实现一致。
- 静态情景核对：文档修正无需 Java 全套测试；代码提交仍须 verify 和覆盖率检查；已有推送授权不重复询问；没有授权的发布需先完成准备再确认；未来设计建议不会自动触发开发；未消除的设计/实现差异需报告。以上为人工规则核对，不是模型行为评测。
- 本次是文档与指令修改，没有新增或重跑 Java 测试。上一轮 205 个测试及覆盖率结果保留在测试覆盖审查中，不作为本次新跑的结果。

局限：本次审计针对会影响 Agent 行为的指令和发现的契约冲突，不是整份设计的逐项实现验收；未开展提示词 A/B 评测，不能据此声称模型完成率、速度或成本已经提升。
